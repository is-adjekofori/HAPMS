# Used by docker-compose.yml to provision the local MySQL container.
# Copy to .env at the repo root and adjust if needed; defaults match
# backend/.env.example's DATABASE_URL out of the box.
MYSQL_DATABASE=hapms
MYSQL_USER=hapms
MYSQL_PASSWORD=hapms
MYSQL_ROOT_PASSWORD=hapms_root
MYSQL_PORT=3306
